
天行电力培训
答案：ABC
解析：I/O接口的基本功能包括设备寻址、数据缓冲、格式转换（如串并转换）、状态检测、控制命令传递等。算术逻辑运算是CPU的功能，不属于I/O接口。
考点：I/O接口的功能。
12.
答案：ABD
解析：中断屏蔽字可动态改变中断处理的优先级（A正确）；不可屏蔽中断（NMI）不受屏蔽字影响（B正确）；每个中断源通常有自己的中断屏蔽字（D正确）。C错误：屏蔽寄存器中某位为1通常表示屏蔽（禁止）该中断，而非允许。
考点：中断屏蔽技术。
13.
答案：ABC
解析：DMA控制器在数据传送期间需要维护内存起始地址（地址计数器）、数据块长度（字节计数器）、设备地址（或I/O地址）。当前指令地址是CPU的程序计数器（PC），与DMA无关。
考点：DMA控制器的工作状态寄存器。
14.
答案：ACD
解析：字节多路通道以字节为单位交叉传输，适合多个低速设备（A正确）；数组多路通道以块为单位交叉传输（C正确）；选择通道物理上可连接多个设备，但一次只能服务一个，其余设备不能并行工作（D正确，B错误）。
考点：通道类型（字节多路、选择、数组多路）的特点。
15.
答案：ABCD
解析：一次完整的总线传输通常包括：总线请求与仲裁、寻址（发送地址）、数据阶段（传输数据）、结束阶段（释放总线）。
考点：总线传输的四个阶段。
三、判断题
1、答案：A
解析：总线包括地址线、数据线和控制线三部分。
考点：总线的组成与特性
2、
答案：A
解析：复用传送是指分时使用总线，如PCI总线中地址和数据传送可以复用32根总线。
考点：总线的组成与特性
3、
答案：A
解析：外设不能直接连接总线，必须通过半导体结构的接口部件。
考点：总线的组成与特性
4、
答案：B
74