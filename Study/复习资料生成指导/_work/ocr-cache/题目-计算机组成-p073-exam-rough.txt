
天行电力培训
负担。
D 错误：DMA 适用于高速、大批量数据传输的设备（如磁盘、网卡），但对低速或零星数据传输的设备优势不明显，且传输速度受总线带宽和 DMA 控制器性能限制，并非“总是高于”中断方式。
考点：I/O 方式
17、
答案：A
解析：$1024 \times 768 \times 8\text{bit} = 768\text{KB}$，256 种颜色需要 8 位存储。
考点：I/O 设备
18、
答案：A
解析：可编程外围接口芯片内部一般有专门记录工作状态的寄存器，CPU 可以通过写入该寄存器来控制芯片的工作模式。
考点：I/O 接口：结构
19、
答案：B
解析：地址信息不用传递，时钟信息也不用传递，读写信息说法不正确，所以排除 ACD。
考点：I/O 接口的结构
20、
答案：B
解析：数据从磁盘读到内存采用 DMA 方式。DMA 的传送过程中的操作顺序：初始化 DMA 控制器以及磁盘(预处理)，从磁盘传输一块数据到内存缓冲区(数据传送)，DMA 控制器发出中断请求，执行“DMA 结束”中断服务程序(后处理)。
考点：DMA 传送方式
21.
答案：C
解析：统一编址方式（内存映射编址）将外设端口地址与内存地址置于同一地址空间，使用访存指令（如 MOV）访问外设。独立编址（端口映射）则需专用 I/O 指令。
考点：I/O 接口的编址方式。
22.
答案：C
解析：中断响应要求新中断的优先级高于当前正在处理的程序（包括中断服务程序）的优先级，否则不能打断。其他选项（开中断、未被屏蔽、当前指令执行完毕）均为正确条件。
考点：中断响应条件。
23.
答案：D
解析：DMA 控制器与 CPU 同时访存时的解决方法包括：停止 CPU 访问主存、DMA 与 CPU 交替访问、周期挪用（周期窃取）。三者都是可行方法，其中周期挪用最常用，但选项 D 包含所有，故选 D。
考点：DMA 与 CPU 的访存冲突解决方式。
24.
答案：A
解析：字节多路通道包含多个子通道，每个子通道服务于一个低速设备，以字节为单位交叉传输。选择通道 71