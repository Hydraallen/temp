# OCR 中间产物 — 数据库 · 第 九 章 并发控制
> 来源：8-3-数据库计算机类讲义.pdf  整章物理页(88, 98)

# ========== 整章完整转写（含知识点 + 专项练习/答案） ==========


===== 物理页 8-3-数据库计算机类讲义-p088 =====


天行电力培训
第九章 并发控制
第九章 知识点
知识点1：并发控制机制的责任
事务是并发控制的基本单位，事务的ACID特性可能遭到破坏的原因之一是多个事务对数据库的并发操作造成的。为了保证事务的隔离性和一致性，数据库管理系统需要对并发操作进行正确调度。
产生数据不一致性的主要原因是并发操作破坏了事务的隔离性。并发控制机制就是要用正确的方式调度并发操作，使一个用户事务的执行不受其他事务的干扰，从而避免造成数据的不一致性。
知识点2：并发操作带来的数据不一致性
不一致性问题包括：丢失修改、不可重复读、读“脏”数据。
（1）丢失修改：两个事务T1和T2读入同一数据并修改，T2的提交结果破坏了T1提交的结果，导致T1的修改被丢失。
（2）不可重复读：事务T1读取数据后，事务T2执行更新操作，使T1无法再现前一次读取结果。
不可重复读包括三种情况：①事务T1读取某一数据后，事务T2对其做了修改，当事务T1再次读该数据时，得到与前一次不同的值。②事务T1按一定条件从数据库中读取了某些数据记录后，事务T2删除了其中部分记录，当T1再次按相同条件读取数据时，发现某些记录神秘地消失了。③事务T1按一定条件从数据库中读取某些数据记录后，事务T2插入了一些记录，当T1再次按相同条件读取数据时，发现多了一些记录。后两种不可重复读有时也称为“幻影现象”。
（3）读“脏”数据
事务T1修改某一数据，并将其写回磁盘；事务T2读取同一数据后，T1由于某种原因被撤销；这时T1已修改过的数据恢复原值，T2读到的数据就与数据库中的数据不一致；T2读到的数据就为“脏”数据，即不正确的数据。
知识点3：封锁
所谓封锁就是事务T在对某个数据对象（例如表、记录等）操作之前，先向系统发出请求，对其加锁。加锁后事务T就对该数据对象有了一定的控制，在事务T释放它的锁之前，其他事务不能更新此数据对象。
知识点4：基本的封锁类型
基本的封锁类型有两种：排他锁（简称X锁）和共享锁（简称S锁）。
排他锁又称为写锁。若事务T对数据对象A加上X锁，则只允许T读取和修改A，其他任何事务都不能再对A加任何类型的锁，直到T释放A上的锁为止。这就保证了其他事务在T释放A上的锁之前不能再读取和修改A。
共享锁又称为读锁。若事务T对数据对象A加上S锁，则事务T可以读A但不能修改A，其他事务只能再对A加S锁，而不能加X锁，直到T释放A上的S锁为止。这就保证了其他事务可以读A，但在T释放A上的S锁之前不能对A做任何修改。
封锁类型相容矩阵
T2  T1  X   S   -
X   N   N   Y
S   N   Y   Y
-   Y   Y   Y
知识点5：封锁协议
在运用X锁和S锁对数据对象加锁时，需要约定一些规则，这些规则为封锁协议。
规则包括何时申请X锁或S锁、持锁时间、何时释放等。对封锁方式制定不同的规则，形成了各种不同的封锁协议。
知识点6：三级封锁协议
86
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p089 =====


知识点10：死锁的诊断
（1）超时法
如果一个事务的等待时间超过了规定的时限，就认为发生了死锁。超时法实现简单，但其不足也很明
87
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p090 =====


天行电力培训
显，一是有可能误判死锁，如事务因为其他原因而使等待时间超过时限，系统会误认为发生了死锁；二是时限若设置得太长，死锁发生后不能及时发现。
（2）等待图法
事务等待图动态地反映了所有事务的等待情况。并发控制子系统周期性地（比如每隔数秒）生成事务等待图，并进行检测。如果发现图中存在回路，则表示系统中出现了死锁。
知识点11：死锁的解除
选择处理死锁代价最小的事务，将其撤消；释放此事务持有的所有的锁，使其它事务能继续运行下去。
知识点12：并发调度的可串行性
多个事务的并发执行是正确的，当且仅当其结果与按某一次序串行地执行这些事务时的结果相同，称这种调度策略为可串行化调度。可串行性是并发事务正确调度的准则。
一个给定的并发调度，当且仅当它是可串行化的，才认为是正确调度。
知识点13：冲突操作
冲突操作是指不同的事务对同一数据的读写操作和写写操作；其他操作是不冲突操作。
知识点14：冲突可串行化的调度
一个调度Sc在保证冲突操作的次序不变的情况下，通过交换两个事务不冲突操作的次序得到另一个调度Sc'，如果Sc'是串行的，称调度Sc是冲突可串行化的调度。
知识点15：两段封锁协议
所谓两段封锁协议是指所有事务必须分两个阶段对数据项加锁和解锁。
（1）在对任何数据进行读、写操作之前，首先要申请并获得对该数据的封锁；
（2）在释放一个封锁之后，事务不再申请和获得任何其他封锁。
所谓“两段”锁的含义是，事务分为两个阶段：
第一阶段是获得封锁，也称为扩展阶段，在这个阶段，事务可以申请获得任何数据项上的任何类型的锁，但是不能释放任何锁；
第二阶段是释放封锁，也称为收缩阶段，在这个阶段，事务可以释放任何数据项上的任何类型的锁，但是不能再申请任何锁。
知识点16：事务遵守两段锁协议是可串行化调度的充分条件，而不是必要条件。
若并发事务都遵守两段锁协议，则对这些事务的任何并发调度策略都是可串行化的；但是，若并发事务的一个调度是可串行化的，不一定所有事务都符合两段锁协议。
知识点17：两段锁协议和防止死锁的一次封锁法的异同之处
一次封锁法要求每个事务必须一次将所有要使用的数据全部加锁，否则就不能继续执行。因此一次封锁法遵守两段锁协议；但是两段锁协议并不要求事务必须一次将所有要使用的数据全部加锁，因此遵守两段锁协议的事务可能发生死锁。
知识点18：封锁的粒度
封锁对象的大小称为封锁粒度。
封锁对象可以是逻辑单元，也可以是物理单元。
封锁粒度与系统的并发度和并发控制的开销密切相关。直观地看，封锁的粒度越大，数据库所能够封锁的数据单元就越少，并发度就越小，系统开销也越小；反之，封锁的粒度越小，并发度较高，但系统开销也就越大。
知识点19：多粒度封锁
如果在一个系统中同时支持多种封锁粒度供不同的事务选择是比较理想的，这种封锁方法称为多粒度封锁。选择封锁粒度时应该同时考虑封锁开销和并发度两个因素，适当选择封锁粒度以求得最优的效果。
知识点20：显式封锁和隐式封锁
显式封锁：应事务的要求直接加到数据对象上的锁；
隐式封锁：该数据对象没有被独立加锁，是由于其上级结点加锁而使该数据对象加上了锁。
88
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p091 =====


天行电力培训
一般地，对某个数据对象加锁，系统要检查该数据对象上有无显式封锁与之冲突：再检查其所有上级结点，看本事务的显式封锁是否与该数据对象上的隐式封锁冲突；还要检查其所有下级结点，看它们的显式封锁是否与本事务的隐式封锁冲突。
知识点21：意向锁
意向锁的含义是如果对一个结点加意向锁，则说明该结点的下层结点正在被加锁；对任一结点加锁时，必须先对它的上层结点加意向锁。
三种常用的意向锁：意向共享锁（IS锁），意向排他锁（IX锁），共享意向排他锁（SIX锁）
（1）IS锁：如果对一个数据对象加IS锁，表示它的后裔结点拟（意向）加S锁。
（2）IX锁：如果对一个数据对象加IX锁，表示它的后裔结点拟（意向）加X锁。
（3）SIX锁：如果对一个数据对象加SIX锁，表示对它加S锁，再加IX锁，即SIX = S + IX。
在具有意向锁的多粒度封锁方法中，任意事务T要对一个数据对象加锁，必须先对它的上层结点加意向锁。申请封锁时应该按自上而下的次序进行，释放封锁时则应该按自下而上的次序进行。
89
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p092 =====


天行电力培训

第九章 专项练习

一、单选题
1.DBMS 的并发控制子系统，保证了事务（ ）的实现。
A.原子性  B.一致性
C.隔离性  D.持久性
2.为了防止一个事务的执行不影响另一个事务，应该采取（ ）。
A.完整性控制  B.访问控制
C.安全性控制  D.并发控制
3.并发操作会带来哪些数据不一致性（ ）。
A.丢失修改、不可重复读、死锁  B.不可重复读、脏读、死锁
C.丢失修改、脏读、死锁  D.丢失修改、不可重复读、脏读
4.两个事务 T1 利 T2 并发操作如下：
时刻 1：T1 读 X=48；
时刻 2：T2 读 X=48；
时刻 3：T1 X=X+10，写回 X；
时刻 4：X=X-2，写回 X。
对于这个并发操作，下面评价正确的是（ ）。
A.该操作丢失了修改  B.该操作不存在问题
C.该操作读“脏”数据  D.该操作不能重复读
5.数据库系统的并发控制的主要方法是采用（ ）机制。
A.拒绝  B.串行
C.封锁  D.并行
6.若事务 T 对数据对象 A 加上（ ），则只允许 T 读取和修改 A，其他任何事务都不能再对 A 加任何类型的
锁，直到 T 释放 A 上的锁为止。
A.S 锁  B.X 锁
C.IX 锁  D.IS 锁
7.基本的封锁类型有排它锁 X 和共享锁 S，关于两种锁的相容性描述不正确的是（ ）。
A.X/X： TRUE  B.S/S： TRUE
C.S/X： FALSE  D.X/S： FALSE
8.封锁协议不包括（ ）。
A.何时释放锁  B.何时申请锁
C.解锁算法  D.持锁时间
9.一级封锁协议能解决的问题（ ）。
A.丢失修改  B.读脏数据
C.不可重复读  D.幻象读
10.（ ）可以防止丢失修改和读“脏”数据，不能保证可重复读。
A.一级封锁协议  B.二级封锁协议
C.三级封锁协议  D.两段锁协议
11.加锁协议中规定“先申请先服务”的原则，可以避免数据库系统出现（ ）。
A.活锁  B.死锁
C.读-写冲突  D.写-写冲突
12.关于“死锁”，下列说法中正确的是（ ）。
A.死锁是操作系统中的问题，数据库操作中不存在

90

CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p093 =====


天行电力培训
B.数据库操作中防止死锁的方法是禁止两个事务同时操作数据库
C.当两个用户竞争相同资源时不会发生死锁
D.只有出现并发操作时才有可能出现死锁
13.解除死锁时，选择处理死锁（ ）的事务，将其撤消；释放此事务持有的所有的锁，使其它事务能继续运行下去。
A.耗时最长 B.耗时最短
C.代价最小 D.代价最大
14.给定的并发调度是“正确调度”是“可串行化”的（ ）。
A.充要条件 B.充分条件
C.必要条件 D.不充分也不必要条件
15.如果有两个事务，同时对数据库中同一数据进行操作，不会因此冲突的操作是（ ）。
A.一个 SELECT，一个 UPDATE B.其中有一个是 DELETE
C.两个都是 UPDATE D.两个都是 SELECT
16.事务遵守两段锁协议是可串行化调度的（ ）。
A.充要条件 B.充分条件
C.必要条件 D.不充分也不必要条件
17.遵守两段锁协议（ ）一次封锁法。
A.一定是 B.一定不是
C.可能是 D.以上都不对
18.已知事务 T1 的封锁序列为：
LOCK S(A)...LOCK S(B)...LOCK X(C)...UNLOCK(B)...UNLOCK (A)...UNLOCK(C);
事务 T2 的封锁序列为：
LOCK S(A)...UNLOCK(A)...LOCK S(B)...LOCK S(C)...UNLOCK(C)...UNLOCK(B),
则遵守两段封锁协议的事务是（ ）。
A.T1 B.T2
C.T1 和 T2 D.没有
19.关系数据库中，以下封锁的对象中最小的封锁粒度是（ ）。
A.数据库 B.关系
C.元组 D.属性值
20.隐式封锁是该数据对象没有被独立加锁，是由于其（ ）加锁而使该数据对象加上了锁。
A.下级节点 B.上级结点
C.左边节点 D.右边节点
二、多选题
1.并发操作带来的数据不一致性包括（ ）。
A.丢失修改 B.丢失查询
C.不可重复读 D.读“脏”数据
2.基本的封锁类型有（ ）。
A.IS 锁 B.X 锁
C.IX 锁 D.S 锁
3.一级封锁协议不能解决的数据不一致性问题包括（ ）。
A.丢失修改 B.不可重复读
C.读脏数据 D.可重复读
4.三级封锁协议能解决的数据不一致性问题包括（ ）。
A.丢失修改 B.不可重复读
91
（右下角二维码及文字：CS 扫描全能王 3亿人都在用的扫描App）

===== 物理页 8-3-数据库计算机类讲义-p094 =====


三、判断题（A.正确 B.错误）
1.多个用户的应用要同时正确运行，需要并发控制；为了保证不丢失数据，需要恢复控制。（ ）
2.数据库的并发控制是为了保证多用户对数据库所做的修改不会影响数据的一致性。（ ）
3.封锁就是事务在对某个数据对象操作之前，先向系统发出请求，对其加锁。（ ）
4.若事务T对数据对象A加上X锁，则其它事务只能再对A加S锁，而不能加X锁，直到T释放A上的S锁。（ ）
5.三级封锁协议不可以防止不可重复读。（ ）
6.先来先服务可以解决活锁。（ ）
7.事务死锁后，不用关机重启也可以解除死锁。（ ）
8.可串行化的调度不一定是正确的调度。（ ）
9.判断一个并发调度是否正确，可以用可串行化概念来解决。（ ）
10.两段锁协议一定会导致死锁。（ ）

92

===== 物理页 8-3-数据库计算机类讲义-p095 =====


天行电力培训
第九章 参考答案及解析
一、单选题
1.C
解析：并发控制子系统保证了事务隔离性的实现。保证了隔离性，进一步也就保证了一致性。并发控制主要是保证了隔离性，如果是单选题，选C；如果是多选题，选项B也应考虑。
考点：并发控制概述
2.D
解析：并发控制保证隔离性，避免事务相互影响。
考点：并发控制概述
3.D
解析：并发控制会带来的问题包括丢失修改、不可重复读、脏读（读脏数据）。死锁不属于不一致性。
考点：并发控制概述
4.A
解析：时刻4的X覆盖掉了时刻3的X，所以时刻3的X丢失了。
考点：并发控制概述
5.C
解析：数据库系统的并发控制的主要方法是采用封锁机制。
考点：封锁
6.B
解析：排他锁又称为写锁。若事务T对数据对象A加上X锁，则只允许T读取和修改A，其他任何事务都不能再对A加任何类型的锁，直到T释放A上的锁为止。这就保证了其他事务在T释放A上的锁之前不能再读取和修改A。
考点：封锁
7.A
解析：X锁不兼容任何锁，因此X/X：TRUE是错误的。
考点：封锁
8.C
解析：何时申请X锁或S锁、持锁时间、何时释放等。
考点：封锁协议
9.A
解析：一级封锁协议是：事务T在修改数据R之前必须先对其加X锁，直到事务结束才释放。事务结束包括正常结束（COMMIT）和非正常结束（ROLLBACK）。一级封锁协议可以防止丢失修改，并保证事务T是可恢复的。
考点：封锁协议
10.B
解析：二级封锁协议不能保证可重复读。
考点：封锁协议
11.A
解析：FIFO（先来先服务）原则，避免系统中出现活锁。
考点：活锁和死锁
12.D
解析：除了操作系统，数据库中也可能存在着死锁的现象，A错误；B选项没有从根本上解决死锁问题；C选项当两个用户竞争相同资源时可能发生死锁。
考点：活锁和死锁
93
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p096 =====


天行电力培训
13.C
解析：选择处理死锁代价最小的事务。
考点：活锁和死锁
14.A
解析：一个给定的并发调度，当且仅当它是可串行化的，才认为是正确调度。
考点：并发调度的可串行性
15.D
解析：SELECT 是查询，只读取不更新，读读操作不冲突。
考点：封锁
16.B
解析：事务遵守两段锁协议是可串行化调度的充分条件，而不是必要条件。
考点：两段锁协议
17.C
解析：两段锁协议并不要求事务必须一次将所有要使用的数据全部加锁，因此遵守两段锁协议的事务可能发生死锁。
考点：两段锁协议
18.A
解析：T2 在第二项 UNLOCK 后，第三项 LOCK，不符合两段锁协议。
考点：两段锁协议
19.D
解析：封锁的对象中最小的封锁粒度是字段。
考点：封锁的粒度
20.B
解析：隐式封锁是该数据对象没有被独立加锁，是由于其上级结点加锁而使该数据对象加上了锁。
考点：封锁
二、多选题
1.ACD
解析：并发操作带来的数据不一致性包括丢失修改、不可重复读、读“脏”数据。
考点：并发控制概述
2.BD
解析：排他锁（简称 X 锁）和共享锁（简称 S 锁）。
考点：封锁
3.BC
解析：一级封锁协议解决的问题是丢失修改，因此不能解决丢失修改和读脏数据的问题。“可重复读”是干扰项，没有这个不一致性问题。
考点：封锁协议
4.ABC
解析：三级封锁协议除了防止丢失修改和读脏数据外，还进一步防止了不可重复读。
考点：封锁协议
5.AC
解析：在数据库中解决死锁问题主要有两类方法：（1）采取一定措施来预防死锁的发生（2）允许发生死锁，采用一定手段定期诊断系统中有无死锁，若有则解除之。
考点：活锁和死锁
6.BC
94

===== 物理页 8-3-数据库计算机类讲义-p097 =====


解析：超时法实现简单，但其不足也很明显，一是有可能误判死锁，如事务因为其他原因而使等待时间超过时限，系统会误认为发生了死锁；二是时限若设置得太长，死锁发生后不能及时发现。
考点：活锁和死锁
7.BC
解析：冲突操作是指不同的事务对同一数据的读写操作和写写操作。
考点：并发调度的可串行性
8.B
解析：事务遵守两段锁协议是可串行化调度的充分条件，而不是必要条件。遵守两段锁协议的事务可能发生死锁。
考点：两段锁协议
9.AD
解析：一次封锁法遵守两段锁协议；但是两段锁协议并不要求事务必须一次将所有要使用的数据全部加锁，因此遵守两段锁协议的事务可能发生死锁。
考点：两段锁协议
10.ACD
解析：封锁对象封锁单元封锁的对象可以是逻辑单元，也可以是物理单元。逻辑单元：属性值、属性值集合、元组、关系、索引项、整个索引、整个数据库等；物理单元：页（数据页或索引页）、块等。
考点：封锁的粒度
三、判断题（A.正确 B.错误）
1.A
解析：题干所述正确。
考点：并发控制概述
2.A
解析：题干所述正确。
考点：并发控制概述
3.A
解析：题干所述正确。
考点：封锁
4.B
解析：X 锁上面不能加 X 锁，也不能加 S 锁。
考点：封锁
5.B
解析：三级封锁协议可以防止不可重复读、丢失修改、读脏数据三类问题。
考点：封锁协议
6.A
解析：题干所述正确。
考点：活锁和死锁
7.A
解析：不需关机，破坏死锁条件即可。
考点：活锁和死锁
8.B
解析：可串行化的调度一定是正确的调度。
考点：并发调度的可串行性
9.A
解析：题干所述正确。

===== 物理页 8-3-数据库计算机类讲义-p098 =====


天行电力培训
考点：并发调度的可串行性
10.B
解析：可能产生死锁，而不是一定产生死锁。
考点：两段锁协议
天行电力培训
96
CS 扫描全能王
3亿人都在用的扫描App
