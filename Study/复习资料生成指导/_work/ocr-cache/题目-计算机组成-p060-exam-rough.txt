
解析：程序局部性原理包括时间局部性（最近访问的数据很可能再次访问）、空间局部性（邻近地址的数据很可能被访问）以及控制流局部性（指令顺序执行概率高于转移）。数据压缩局部性不属于经典局部性原理。  
考点：局部性原理的内容。  
23.  
答案：B  
解析：多级存储器的包含性原则（inclusion property）要求：内层（上层）存储器中的信息一定是外层（下层）存储器中信息的子集，即内层包含在外层中。因此内层存储器的信息一定包含在外层存储器中。  
考点：存储器层次结构的包含性原则。  
24.  
答案：C  
解析：DRAM存储单元基于电容电荷，读操作时电荷经位线放电导致原有电荷丢失，因此读出后数据被破坏（清除），必须立即写回（预充电后重写），称为破坏性读出。  
考点：DRAM的破坏性读出原理  
25.  
答案：C  
解析：DRAM刷新以行为单位，一次刷新选中一行，该行所有存储单元同时刷新。这是因为DRAM按行共享字线，行内各列同时动作。  
考点：DRAM的刷新方式。  
26.  
答案：B  
解析：交叉存取度\( m \)定义为存储模块的存取周期\( T \)与总线传输周期\( \tau \)之比，即\( m = T / \tau \)（通常取整数），表示在\( T \)时间内可连续启动\( m \)个模块。  
考点：多体交叉存储器的交叉存取度。  
27.  
答案：B  
解析：全相联映像允许主存中的任意一个块映射到Cache中的任意位置，灵活性最高，但硬件实现复杂。直接映像固定映射，组相联映像限制在组内。  
考点：Cache的映像方式  

二、多选题  
1、  
答案：ABD  
解析：A错误：主存容量的基本单位是字节（Byte），而不是字长。字长是CPU处理数据的宽度。  
B错误：主存容量指存储单元的总字节数，而非芯片数量。  
C正确：主存容量通常以字节（B）、千字节（KB）、兆字节（MB）、吉字节（GB）等单位表示；同时，地址总线的位数决定了CPU可寻址的内存空间大小，进而影响最大支持容量。  
D错误：主存容量与CPU时钟频率无直接关系，时钟频率主要影响运算速度。  
考点：概念  
2、  
答案：AB  
解析：三级存储层次一般是cache、主存和辅存。  
考点：存储层次  
3、  
答案：AC  
解析：随机存取存储器，如DRAM信息掉电丢失。半导体存储器有易失DRAM和非易失SRAM和ROM等两  
58