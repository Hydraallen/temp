
7、以太网使用的( )编码方式。  
A、不归零制 B、归零制 C、曼彻斯特 D、原比特流  
8、下列列举的接口中属于逻辑接口的是( )。  
A、AUI接口 B、V.24接口 C、SC接口 D、Loopback接口  
9、网卡工作在OSI/RM的( )。  
A、物理层B、数据链路层  
C、物理层和数据链路层D、数据链路层和网络层  
10、以太网交换机根据( )转发数据帧。  
A、IP地址B、MAC地址C、LLC地址D、PORT地址  
11、下列网络设备中工作在数据链路层的是( )。  
A、中继器B、网卡C、集线器D、网桥  
12、下面( )设备可以被看作多端口的网桥设备。  
A、中继器B、交换机C、路由器D、集线器  
13、默认情况下，交换机收到一个未知单播帧之后，会执行( )操作。  
A、学习该数据帧的源MAC地址与接口对应关系，丢弃  
B、学习该数据帧的源MAC地址与接口对应关系，泛洪  
C、学习该数据帧的源MAC地址与接口对应关系，丢弃  
D、不学习该数据帧的源MAC地址与接口对应关系，丢弃  
14、VLAN之间的通信需要( )设备。  
A、网桥 B、二层交换机 C、路由器 D、集线器  
15、网桥从一个端口收到正确的数据帧后，在其地址转发表中查找该帧要到达的目的站，若要到达的目的站仍然在该端口上，则会( )。  
A、丢弃此帧 B、向该端口转发此帧  
C、将此帧作为地址探测帧 D、利用此帧建立该端口的地址转换  
16、局域交换机首先完整地接收一个数据帧，然后根据校验结果确定是否转发，这种交换方法叫做( )。  
A、直通式交换B、存储转发交换C、碎片隔离交换D、查询交换  
17、以下( )接口是双绞线的接口。  
A、AUI接口B、BNC接口C、RJ-45接口D、FC接口  
18、对于使用交换机连接起来的10Mbit/s的共享式以太网，若有10个用户，则每个用户能够占有的带宽为( )。  
A、1Mbit/s B、2Mbit/s C、10Mbit/s D、100Mbit/s  
10