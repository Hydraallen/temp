# OCR 中间产物 — 数据库 · 第 四 章 关系数据库标准语言SQL
> 来源：8-3-数据库计算机类讲义.pdf  整章物理页(33, 46)

# ========== 整章完整转写（含知识点 + 专项练习/答案） ==========


===== 物理页 8-3-数据库计算机类讲义-p033 =====


天行电力培训
第四章 关系数据库标准语言SQL
第四章 知识点
知识点1：结构化查询语言
结构化查询语言（Structured Query Language，SQL）是关系数据库的标准语言，也是一个通用的、功能极强的关系数据库语言。其功能不仅仅是查询，而是包括数据库模式创建、数据库数据的插入与修改、数据库安全性完整性定义与控制等一系列功能。
知识点2：SQL的基本概念
支持SQL的关系数据库管理系统同样支持关系数据库三级模式结构。其中外模式包括若干视图和部分基本表，数据库模式包括若干基本表，内模式包括若干存储文件。用户可以用SQL对基本表和视图进行查询或其他操作，基本表和视图一样，都是关系。
基本表是本身独立存在的表，在关系数据库管理系统中一个关系就对应一个基本表。一个或多个基本表对应一个存储文件，一个表可以带若干索引，索引也存放在存储文件中。
存储文件的逻辑结构组成了关系数据库的内模式。存储文件的物理结构对最终用户是隐蔽的。
视图是从一个或几个基本表导出的表。它本身不独立存储在数据库中，即数据库中只存放视图的定义而不存放视图对应的数据。这些数据仍存放在导出视图的基本表中，因此视图是一个虚表。视图在概念上与基本表等同，用户可以在视图上再定义视图。
SQL
视图1 视图2
基本表1 基本表2 基本表3 基本表4
存储文件1 存储文件2
外模式 模式 内模式
知识点3：SQL语言的特点
（1）综合统一。集数据定义语言、数据操纵语言、数据控制语言的功能于一体，语言风格统一，可以独立完成数据库生命周期中的全部活动。
（2）高度非过程化。进行数据操作时，只要提出“做什么”，而无须指明“怎么做”，无须了解存取路径。
（3）面向集合的操作方式。采用集合操作方式，不仅操作对象、查找结果可以是元组的集合，而且一次插入、删除、更新操作的对象也可以是元组的集合。
（4）一种语法，多种方式。SQL既是独立的语言，又是嵌入式语言。作为独立的语言，它能够独立地用于联机交互的使用方式；作为嵌入式语言，SQL语句能够嵌入到高级语言程序中。
（5）语言简洁。完成核心功能只用了9个动词。SQL接近英语口语，因此易于学习和使用。
SQL的动词
SQL功能 动词
数据查询 SELECT
数据定义 CREATE，DROP，ALTER
数据操纵 INSERT，UPDATE，DELETE
数据控制 GRANT，REVOKE
知识点4：数据定义
包括模式定义、表定义、视图和索引的定义。
31
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p034 =====


天行电力培训
一个关系数据库管理系统的实例中可以建立多个数据库，一个数据库中可以建立多个模式，一个模式下通常包括多个表、视图和索引等数据库对象。
SQL的数据定义语句表
| 操作对象 | 操作方式 |        |        |
|----------|----------|--------|--------|
|          | 创建     | 删除   | 修改   |
| 模式     | CREATE SCHEMA | DROP SCHEMA |        |
| 表       | CREATE TABLE | DROP TABLE | ALTER TABLE |
| 视图     | CREATE VIEW | DROP VIEW |        |
| 索引     | CREATE INDEX | DROP INDEX | ALTER INDEX |

知识点5：模式的定义与删除
定义模式 CREATE SCHEMA<模式名>AUTHORIZATION<用户名>；
删除模式 DROP SCHEMA <模式名><CASCADE | RESTRICT>；

知识点6：基本表的相关操作
（1）定义基本表
CREATE TABLE <表名>
（<列名><数据类型>[<列级完整性约束条件>]
[，<列名><数据类型>[<列级完整性约束条件>]]
…
[，<表级完整性约束条件>]）：

（2）修改基本表
ALTER TABLE <表名>
[ ADD[COLUMN] <新列名><数据类型>[完整性约束] ]
[ ADD <表级完整性约束>]
[ DROP [ COLUMN ] <列名> [CASCADE| RESTRICT] ]
[ DROP CONSTRAINT<完整性约束名>[ RESTRICT | CASCADE ] ]
[ ALTER COLUMN <列名><数据类型> ]；

（3）删除基本表
DROP TABLE <表名> [RESTRICT|CASCADE] ；

知识点7：索引
常见索引有顺序文件上的索引、B+树索引、散列（hash）索引、位图索引等。索引虽然能够加速数据库查询，但需要占用一定的存储空间，当基本表更新时，索引要进行相应的维护，这些都会增加数据库的负担，因此要根据实际应用的需要有选择地创建索引。

关系数据库管理系统在执行查询时会自动选择合适的索引作为存取路径，用户不必也不能显式地选择索引。索引是关系数据库管理系统的内部实现技术，属于内模式的范畴。

知识点8：索引的建立与删除
（1）建立索引
CREATE [UNIQUE] [CLUSTER] INDEX <索引名>
ON <表名>（<列名>[<次序>][, <列名>[<次序>]…]）；

（2）修改索引
ALTER INDEX <旧索引名> RENAME TO <新索引名>

（3）删除索引
DROP INDEX <索引名>；

知识点9：数据字典
数据字典是关系数据库管理系统内部的一组系统表，它记录了数据库中所有的定义信息，包括关系模
32
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p035 =====


式定义、视图定义、索引定义、完整性约束定义、各类用户对数据库的操作权限、统计信息等。关系数据库管理系统在执行SQL的数据定义语句时，实际上就是在更新数据字典表中的相应信息。在进行查询优化和查询处理时，数据字典中的信息是其重要依据。

知识点10：数据查询  
数据查询是数据库的核心操作。  
SQL提供了SELECT语句进行数据查询。其一般格式为：  
SELECT [ALL|DISTINCT] <目标列表表达式>[, <目标列表表达式>] …  
FROM <表名或视图名>[, <表名或视图名>]…| (SELECT 语句) [AS] <别名>  
[WHERE <条件表达式>]  
[GROUP BY <列名1> [HAVING <条件表达式>]]  
[ORDER BY <列名2> [ASC|DESC]];  

知识点11：单表查询  
单表查询是指仅涉及一个表的查询。  
（1）选择表中的若干列 ①查询指定列 ②查询全部列  
（2）选择表中的若干个元组 ①消除取值重复的行 ②查询满足条件的元组  
查询满足指定条件的元组可以通过WHERE子句实现。  

常用的查询条件  

| 查询条件       | 谓词                                                                 |
|----------------|----------------------------------------------------------------------|
| 比较           | =, >, <, >=, <=, !=, <>, |>, |<; NOT+上述比较运算符                         |
| 确定范围       | BETWEEN AND, NOT BETWEEN AND                                          |
| 确定集合       | IN, NOT IN                                                           |
| 字符匹配       | LIKE, NOT LIKE                                                       |
| 空值           | IS NULL, IS NOT NULL                                                  |
| 多重条件（逻辑运算） | AND, OR, NOT                                                         |

谓词LIKE可以用来进行字符串的匹配。  
通配符%（百分号）代表任意长度的字符串。通配符_（下横线）代表任意单个字符。  

（3）ORDER BY子句。用户可以用ORDER BY子句对查询结果按照一个或多个属性列的升序（ASC）或降序（DESC）排列，默认值为升序。  

（4）聚集函数。当聚集函数遇到空值时，除COUNT(*)外，都跳过空值而只处理非空值。COUNT（*）是对元组进行计数，某个元组的一个或部分列取空值不影响COUNT的统计结果。注意，WHERE子句中是不能用聚集函数作为条件表达式的。聚集函数只能用于SELECT子句和GROUP BY中的HAVING子句。  

SQL主要的聚集函数  

| 函数                          | 功能                     |
|-------------------------------|--------------------------|
| COUNT（*）                     | 统计元组个数             |
| COUNT（[DISTINCT|ALL] <列名>）  | 统计一列中值的个数       |
| SUM（[DISTINCT|ALL] <列名>）    | 计算一列值的总和（此列必须是数值型） |
| AVG（[DISTINCT|ALL] <列名>）    | 计算一列值的平均值（此列必须是数值型） |
| MAX（[DISTINCT|ALL] <列名>）    | 求一列值中的最大值       |
| MIN（[DISTINCT|ALL] <列名>）    | 求一列值中的最小值       |

（5）GROUP BY子句。GROUP BY子句将查询结果按某一列或多列的值分组，值相等的为一组。对查询结果分组的目的是为了细化聚集函数的作用对象。如果分组后还要求按一定的条件对这些组进行筛选，最终只输出满足指定条件的组，则可以使用HAVING短语指定筛选条件。  

知识点12：连接查询  
若一个查询同时涉及两个以上的表，则称之为连接查询。  
连接查询是关系数据库中最主要的查询，包括等值连接查询、自然连接查询、非等值连接查询、自身

===== 物理页 8-3-数据库计算机类讲义-p036 =====


天行电力培训
连接查询、外连接查询和复合条件连接查询、多连接等。
连接查询的 WHERE 子句中用来连接两个表的条件称为连接条件或连接谓词。连接条件中的列名称称为连接字段。
一般格式为: [<表名1>]<列名1> <比较运算符> [<表名2>]<列名2>
还可以使用: [<表名1>]<列名1> BETWEEN [<表名2>]<列名2> AND [<表名2>]<列名3>
当连接运算符为=时，称为等值连接。使用其他运算符称为非等值连接。
知识点13: 自身连接
一个表与其自己进行连接。
需要给表起别名以示区别。因为所有属性名都是同名属性，必须使用“别名”。
知识点14: 外连接
外连接 OUTER JOIN，左外连接 LEFT OUTER JOIN，右外连接 RIGHT OUTER JOIN。
知识点15: 多表连接
两个以上的表进行连接。
知识点16: 嵌套查询
在 SQL 语言中，一个 SELECT-FROM-WHERE 语句称为一个查询块。将一个查询块嵌套在另一个查询块的 WHERE 子句或 HAVING 短语的条件中的查询称为嵌套查询。
嵌套查询使用户可以用多个简单查询构成复杂的查询，从而增强 SQL 的查询能力。以层层嵌套的方式来构造程序正是 SQL 中“结构化”的含义所在。
知识点17: 父查询、子查询
嵌套查询上层的查询块称为外层查询或父查询，下层查询块称为内层查询或子查询。
SQL 语言允许多层嵌套查询，即：一个子查询中还可以嵌套其他子查询。
子查询的 SELECT 语句中不能使用 ORDER BY 子句，ORDER BY 子句只能对最终查询结果排序。
不相关子查询：子查询的查询条件不依赖于父查询。
相关子查询：子查询的查询条件依赖于父查询。
知识点18: 常用的子查询
带有 IN 谓词的子查询、带有比较运算的子查询、带有 ANY/ALL 谓词的子查询、带有 EXISTS 谓词的子查询。
知识点19: 集合查询
SELECT 语句的查询结果是元组的集合，所以多个 SELECT 语句的结果可进行集合操作。
集合操作主要包括：并 UNION、交 INTERSECT、差 EXCEPT。
知识点20: 基于派生表的查询
子查询不仅可以出现在 WHERE 子句中，还可以出现在 FROM 子句中，这时子查询生成的临时派生表成为主查询的查询对象。
知识点21: 数据更新
数据更新操作有三种：插入数据 INSERT、修改数据 UPDATE、删除数据 DELETE。
知识点22: 插入数据
插入语句 INSERT 通常有两种形式，一种是插入一个元组，另一种是插入子查询结果。
(1) 插入元组
INSERT INTO <表名> [ (<属性列1>[, <属性列2>... ) ] VALUES ( <常量1>[, <常量2>]... ) ;
(2) 插入子查询结果
INSERT INTO <表名> [ (<属性列1>[, <属性列2>... ) ] 子查询;
知识点23: 修改数据
修改操作又称为更新操作，其语句的一般格式为:
UPDATE <表名> SET <列名>=<表达式>[, <列名>=<表达式>]... [WHERE <条件>];

34
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p037 =====


天行电力培训
其功能是修改指定表中满足 WHERE 子句条件的元组。其中 SET 子句给出<表达式>的值用于取代相应的属性列值。如果省略 WHERE 子句，则表示要修改表中的所有元组。
三种修改方式：修改某一个元组的值，修改多个元组的值，带子查询的修改语句。
知识点24：删除数据
删除语句的一般格式为：DELETE FROM <表名> [WHERE <条件>]；
DELETE 语句的功能是从指定表中删除满足 WHERE 子句条件的所有元组。如果省略 WHERE 子句则表示删除表中全部元组，但表的定义仍在字典中。也就是说 DELETE 语句删除的是表中的数据，而不是关于表的定义，注意与 DROP 的区别。
三种删除方式：删除某一个元组的值，删除多个元组的值，带子查询的删除语句。
知识点25：空值的处理
所谓空值就是“不知道”或“不存在”或“无意义”的值。
一般有以下几种情况：该属性应该有一个值，但目前不知道它的具体值；该属性不应该有值；由于某种原因不便于填写。
因此空值是很特殊的值，含有不确定性，对关系运算带来特殊的问题，需要做特殊的处理。
相关操作：（1）空值的产生（2）空值的判断（3）空值的约束条件（4）空值的运算。
知识点26：视图
视图是从一个或几个基本表（或视图）导出的表。它与基本表不同，是一个虚表。
数据库中只存放视图的定义，而不存放视图对应的数据，这些数据仍存放在原来的基本表中。
基本表中的数据发生变化，从视图中查询出的数据也就随之改变。
视图一经定义，就可以和基本表一样被查询、被删除。
可以在一个视图之上再定义新的视图，但对视图的更新（增、删、改）操作则有一定的限制。
知识点27：建立视图
CREATE VIEW <视图名> [（<列名> [，<列名> ] ）] AS <子查询> [WITH CHECK OPTION]；
WITH CHECK OPTION 表示对视图进行 UPDATE、INSERT 和 DELETE 操作时要保证更新、插入或删除的行满足视图定义中的谓词条件（即子查询中的条件表达式）。
组成视图的属性列名或者全部省略或者全部指定，没有第三种选择。如果省略了视图的各个属性列名，则隐含该视图由子查询中 SELECT 子句目标列中的诸字段组成。
下列以下三种情况下必须明确指定组成视图的所有列名：
（1）某个目标列不是单纯的属性名，而是聚集函数或列表达式
（2）多表连接时选出了几个同名列作为视图的字段
（3）需要在视图中为某个列启用新的更合适的名字
关系数据库管理系统执行 CREATE VIEW 语句的结果只是把视图的定义存入数据字典，并不执行其中的 SELECT 语句。只是在对视图查询时，按视图的定义从基本表中将数据查出。
视图可以建立在单个基本表上，建立在多个基本表上，建立在一个或多个已定义好的视图上，还可以建立在基本表与视图上。
知识点28：行列子集视图
若一个视图是从单个基本表导出的，并且只是去掉了基本表的某些行和某些列，但保留了主码，我们称这类视图为行列子集视图。
知识点29：删除视图
DROP VIEW <视图名>[CASCADE]；
视图删除后视图的定义将从数据字典中删除。如果该视图上还导出了其他视图，则使用 CASCADE 级联删除语句把该视图和由它导出的所有视图一起删除。
基本表删除后，由该基本表导出的所有视图均无法使用了，但是视图的定义没有从字典中清除。删除这些视图定义需要显式地使用 DROP VIEW 语句。
35
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p038 =====


天行电力培训
知识点30：查询视图
用户就可以像对基本表一样对视图进行查询。
知识点31：视图消解
关系数据库管理系统执行对视图的查询时，首先进行有效性检查，检查查询中涉及的表、视图等是否存在。如果存在，则从数据字典中取出视图的定义，把定义中的子查询和用户的查询结合起来，转换成等价的对基本表的查询，然后再执行修正了的查询。这一转换过程称为视图消解。
知识点32：更新视图
更新视图是指通过视图来插入（INSERT）、删除（DELETE）和修改（UPDATE）数据。
由于视图是不实际存储数据的虚表，因此对视图的更新最终要转换为对基本表的更新。
对视图的更新操作也是通过视图消解，转换为对基本表的更新操作。为防止用户通过视图对数据进行增加、删除、修改时，有意无意地对不属于视图范围内的基本表数据进行操作，可在定义视图时加上 WITH CHECK OPTION 子句。关系数据库管理系统会检查视图定义中的条件，若不满足条件则拒绝执行该操作。
知识点33：更新视图的注意事项
（1）若视图是由两个以上基本表导出的，则此视图不允许更新。
（2）若视图的字段来自字段表达式或常数，则不允许对此视图执行 INSERT 和 UPDATE 操作，但允许执行 DELETE 操作。
（3）若视图的字段来自聚集函数，则此视图不允许更新。
（4）若视图定义中含有 GROUP BY 子句，则此视图不允许更新。
（5）若视图定义中含有 DISTINCT 短语，则此视图不允许更新。
（6）若视图定义中有嵌套查询，并且内层查询的 FROM 子句中涉及的表也是导出该视图的基本表，则此视图不允许更新。
知识点34：视图的作用
（1）视图能够简化用户的操作
（2）视图使用户能以多种角度看待同一数据
（3）视图对重构数据库提供了一定程度的逻辑独立性
（4）视图能够对机密数据提供安全保护
（5）适当利用视图可以更清晰地表达查询
36
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p039 =====


天行电力培训
第四章 专项练习
一、单选题
1.DBMS提供DML实现对数据的操作，嵌入高级语言中使用的DML称为（ ）。
A.自主型  B.自含型
C.宿主型  D.交互型
2.SQL语句INSERT、DELETE、UPDATE实现了（ ）功能。
A.数据查询  B.数据操控
C.数据定义  D.数据控制
3.名为C的表，需要添加一个名为D的新列，应该使用（ ）语句。
A.MODIFY TABLE C MODIFY (D INTEGER)
B.ALTER TABLE C ADD (D INTEGER)
C.MODIFY TABLE C ADD (D INTEGER)
D.ALTER TABLE C MODIFY (D INTEGER)
4.SQL语句创建表SC：
CREATE TABLE SC
(S# CHAR (6) NOT NULL,
C# CHAR (3) NOT NULL,
SCORE INTEGER,
NOTE CHAR (20));
向SC表插入如下行时，（ ）可以被插入。
A.(201009','101',60,必修)  B.(200823','101',NULL,NULL)
C.(NULL,'103',80,选修，)  D.(20032',NULL,86,')
5.根据（ ）定义分配给列中的值的存储空间量。
A.数据类型  B.字体
C.数据结构  D.ASCII码
6.学生表中的“学号”由9位数字组成，使用（ ）数据类型更合适。
A.INT  B.CHAR
C.DATE  D.SMALLINT
7.SELECT语句执行的结果是（ ）。
A.数据项  B.元组
C.表  D.数据库
8.设关系R(A,B,C)，与SQL语句“SELECT A FROM R WHERE C='男'”等价的关系代数表达式是（ ）。
A.Π_A(σ_{C='男'}(R))  B.σ_{C='男'}(Π_A(R))
C.σ_{C='男'}(Π_{A,C}(R))  D.Π_A(σ_{C='男'}(R))
9.SELECT的条件“年龄 BETWEEN 20 AND 25”，表示年龄在20至25之间，且（ ）。
A.包括20岁和25岁  B.不包括20岁和25岁
C.包括20岁但不包括25岁  D.包括25岁但不包括20岁
10.SELECT语句中，若希望查询的结果不出现重复元组，则应使用（ ）保留字。
A.DISTINCT  B.ALL
C.LIKE  D.TOP
11.下面是聚合函数的选项是（ ）。
A.DISTINCT  B.SUM
C.IF  D.TOP
12.在SELECT语句中使用ORDER BY子句排序，（ ）选项表示升序。
37
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p040 =====


A.DESC
B.ASC
C.UP
D.DOWN
13.SELECT 语句中，WHERE 子句的条件表达式中，字符串匹配的操作符是（ ）。
A.DISTINCT
B.ALL
C.LIKE
D.TOP
14.在 SELECT 查询语句中的 LIKE，“DB_”表示（ ）的字符串。
A.长度为 3 的以“DB”开头
B.长度为 2 的以“DB”开头
C.任意长度的以“DB”开头
D.长度为 3 的以“DB”开头第三个字符为“_”
15.若想查询出所有姓张的、且出生日期为空的学生信息，则 WHERE 条件应为（ ）。
A.姓名 LIKE '张%' AND 出生日期=NULL
B.姓名 LIKE '张*' AND 出生日期=NULL
C.姓名 LIKE '张%' AND 出生日期 IS NULL
D.姓名 LIKE '张_' AND 出生日期 IS NULL
16.候选人表 CANDIDATE，查询候选人所在城市(CITY)，合适的 SQL 是（ ）。
A.SELECT CITY FROM CANDIDATE ORDER BY CITY
B.SELECT CITY FROM CANDIDATE
C.SELECT DISTINCT * FROM CANDIDATE
D.SELECT DISTINCT CITY FROM CANDIDATE
17.使用 SQL 语句进行分组检索时，为了去掉不满足条件的分组，应该（ ）。
A.使用 WHERE 子句
B.在 GROUP BY 后面使用 HAVING 子句
C.先使用 WHERE 子句，再使用 HAVING 子句
D.先使用 HAVING 子句，再使用 WHERE 子句
18.学生 S(S#, SNAME, SEX, DEPARTMENT)；课程 C(C#, CNAME, TEACHER)；选课关系 SC(S#, C#, GRADE)，“查询选修了 3 门以上课程的学生的学号”的 SQL 语句是（ ）。
A.SELECT S# FROM SC GROUP BY S# WHERE COUNT(*)>3
B.SELECT S# FROM SC GROUP BY S# HAVING COUNT(*)>3
C.SELECT S# FROM SC ORDER BY S# WHERE COUNT(*)>3
D.SELECT S# FROM SC ORDER BY S# HAVING COUNT(*)>3
19.关系 R(A, B, C)和 S(A, D, E, F)，有 R.A=S.A。若将关系代数表达式∏_{R.A,R.B,S.D,S.F}(R⋈S)用 SQL 语言的查询语句表示，则为：（ ）。
A. SELECT R.A,R.B,S.D,S.F FROM R, S WHERE R.A=S.D
B. SELECT R.A,R.B,S.D,S.F FROM R, S WHERE R.A=S.A
C. SELECT R.A,R.B,S.D,S.F FROM R, S WHERE R.B=S.A
D. SELECT R.A,R.B,S.D,S.F FROM R, S WHERE R.C=S.A
20.学生 S(S#, SNAME, SEX, AGE)，课程 C(C#, CNAME, TEACHER)，选课 SC(S#, C#, GRADE)。查找某个学生的基本信息及其选课的平均成绩，将使用关系（ ）。
A.S 和 SC
B.SC 和 C
C.S 和 C
D.S、SC 和 C
21.当表 R 和表 S 左外连接时，查询的结果集中能够包含（ ）。
A.R 中的部分记录，S 中的全部记录
B.R 中的全部记录，S 中的全部记录
C.R 中的全部记录，S 中的部分记录
D.R 中的部分记录，S 中的部分记录
22.在 SQL 语言中，子查询是（ ）。
A.返回单表中数据子集的查询语言
B.选取多表中字段子集的查询语句
38

===== 物理页 8-3-数据库计算机类讲义-p041 =====


天行电力培训
C.选取单表中字段子集的查询语句 D.嵌入到另一个查询语句之中的查询语句
23.在学生表 STUDENT 的系别（SDEPT）属性中查询信息系（IS）、数学系（MA）和计算机系（CS）的
学生姓名（SNAME）和性别（SSEX），正确的命令格式应为（）。
A.SELECT STUDENT FROM SNAME, SSEX WHERE SDEPT IN ('IS','MA','CS')
B.SELECT SNAME, SSEX FROM STUDENT WHERE SDEPT IN (IS,MA,CS)
C.SELECT SNAME, SSEX FROM STUDENT WHERE SDEPT IN ('IS','MA','CS')
D.SELECT SNAME, SSEX FROM STUDENT WHERE SDEPT LIKE IS, MA, CS
24.下述 SQL 语句中更新表中数据作用的命令动词是（）。
A.ALTER B.CREATE
C.UPDATE D.INSERT
25.表 S(SN,CN,GRADE)，SN 为学生名，CN 为课程名，均为字符型；GRADE 为成绩，数值型，取值范围
是 0-100。选项（）是正确的插入语句。
A.ADD INTO S VALUES('张三', '化学', 98)
B.INSERT INTO S VALUES （'张三', '化学', '80')
C.INSERT INTO S VALUES （'张三', '化学', 98）
D.INSERT INTO S VALUES （'张三', '化学', 110）
26.下列对 DELETE 权限的叙述中，正确的是（）。
A.允许删除数据 B.允许删除关系
C.允许删除数据库模式 D.与 DROP 权限等价
27.关系 SC（SNO, CNAME, GRADE）,各属性的含义分别为学号、课程名、成绩。若要将所有学生的“数据
库系统”课程的成绩改为 0，能正确完成该操作的 SQL 语句是（）。
A.UPDATE SET SC GRADE = 0 WHERE CNAME='数据库系统'
B.DELETE FROM SC WHERE CNAME='数据库系统'
C.DELETE SC WHERE CNAME='数据库系统'
D.UPDATE SC SET GRADE = 0 WHERE CNAME='数据库系统'
28.删除数据库的方法是（）。
A.使用 DROP DATABASE 语句 B.使用 DELETE TABLE 语句
C.使用 ALTER DATABASE 语句 D.使用 DROP TABLE 语句
29.要求视图的更新必须满足查询中的条件，则需要（）。
A.WITH CHECK OPTION B.WITH UPDATE
C.WITH DELETE D.WITH INSERT
30.视图可用于（）。
A.确保引用完整性
B.保存存储在一个单独表中的数据的额外副本
C.限制对表中特定行或列中的数据的访问
D.在从基本表中删除历史数据前将其保存
二、多选题
1.下列的 SQL 语句中，（）不是数据定义语句。
A.CREATE B.ROLLBACK
C.DROP D.GRANT
2.定义一个列至少需要（）。
A.数据类型 B.索引
C.键 D.名称
3.查询数据可以来自（）。
39
[二维码] CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p042 =====


天行电力培训
A.一个表        B.多个表
C.多个视图      D.一个视图
4.（ ）不属于比较运算符。
A.=
B.AND
C.!=
D.IN
5.查询员工工资信息时，结果按工资降序排列，不正确的是（ ）。
A.ORDER BY 工资
B.ORDER BY 工资 DESC
C.ORDER BY 工资 ASC
D.ORDER BY 工资 DICTINCT
6.执行查询 LIKE '_A%'，可能在查询结果中的是（ ）。
A.ABCDE
B.BACDE
C.BAABB
D.CCADE
7.SELECT STUDENT.NAME, ENROLLMENT.CLASSNAME FROM STUDENT LEFT JOIN ENROLLMENT ON STUDENT.SID = ENROLLMENT.SID；下面描述错误的是（ ）。
A.来自 Student 表的字段可能出现空值，来自 Enrollment 表的字段不会出现空值
B.来自 Student 表的字段不会出现空值，来自 Enrollment 表的字段不会出现空值
C.来自 Student 表的字段不会出现空值，来自 Enrollment 表的字段可能出现空值
D.来自两个表的字段都可能出空值
8.用 UNION 合并两个 SELECT 查询的结果时，正确的叙述是（ ）。
A.查询结果是两个查询中的公共值
B.两个 SELECT 语句必须输出同样的列数
C.两个表各相应列的数据类型必须相同
D.将来自不同查询的数据组合起来
9.学生成绩表 GRADE 中有字段 SCORE（FLOAT），把所有在 55 分至 60 之间的分数提高 5 分，以下 SQL 语句正确的是（ ）。
A.UPDATE GRADE SET SCORE=SCORE+5
B.UPDATE GRADE SET SCORE=SCORE+5 WHERE SCORE>=55 OR SCORE<=60
C.UPDATE GRADE SET SCORE=SCORE+5 WHERE SCORE BETWEEN 55 AND 60
D.UPDATE GRADE SET SCORE=SCORE+5 WHERE SCORE>=55 AND SCORE<=60
10.下面关于视图的说法正确的是（ ）。
A.视图是由若干表组成的，独立存储在数据库中
B.视图的存在提高了并发程度
C.视图与基本表的最大区别在于它是逻辑定义的虚表
D.视图简化了用户数据查看方式，但不提高查询效率
三、判断题（A.正确 B.错误）
1.SQL 语句不能够嵌入到高级语言程序中。（ ）
2.SQL 的数据定义功能包括对数据的增、删、改、查。（ ）
3.索引能够加速数据库查询，因此多建立几个索引有利于提高查询效率。（ ）
4.对一个表做 SELECT 操作后，新表的元组个数小于或等于原表的元组个数。（ ）
5.一个表可以与自己进行连接查询。（ ）
6.所谓相关子查询就是子查询的查询条件不依赖于父查询。（ ）
7.SQL 的集合操作主要包括：选择、投影、连接、并、交、差。
8.两表间的关系删除的同时，这两个表也被自动删除。（ ）
9.DBMS 对视图进行查询时首先进行有效性检查，检查被查询的表、视图等是否存在。（ ）
10.视图是一个虚拟表，并不表示任何物理数据，而只是用来查看数据的窗口而已。（ ）
40

===== 物理页 8-3-数据库计算机类讲义-p043 =====


天行电力培训
第四章 参考答案及解析
一、单选题
1.C
解析：DBMS：数据库管理系统，DML：数据操控语言。被嵌入的高级语言称为宿主语言。
考点：SQL基本概念、特点
2.B
解析：数据操控：INSERT、DELETE, UPDATE 对应着插入、删除、修改。
考点：SQL基本概念、特点
3.B
解析：ALTER TABLE <表名>[ ADD[COLUMN] <新列名> <数据类型> [完整性约束] ]
考点：数据定义（模式、基本表、索引、数据字典）
4.B
解析：A 选项“必修”需要加单引号，C 选项第一项不能为空，D 选项第二个不能为空。
考点：数据定义（模式、基本表、索引、数据字典）
5.A
解析：不同的数据类型占用不同的存储空间。
考点：数据定义（模式、基本表、索引、数据字典）
6.B
解析：“学号”虽然是数字形式，本质上是字符串。用字符类型表示更合适。
考点：数据定义（模式、基本表、索引、数据字典）
7.C
解析：查询语句针对表操作，查询结果为表。
考点：单表查询
8.D
解析：WHERE“查询条件”等价于选择（σ），SELECT“A”表示保留 A 列，等价于投影（Π）。
考点：单表查询
9.A
解析：BETWEEN ...... AND...... 包含边界值。
考点：单表查询
10.A
解析：WHERE 子句的条件表达式中，去重的操作符是 DISTINCT。
考点：单表查询
11.B
解析：SUM（）是聚合函数。
考点：单表查询
12.B
解析：A 选项为降序，B 选项为升序。
考点：单表查询
13.C
解析：在 SQL 查询中使用 LIKE 进行字符串匹配。
考点：单表查询
14.A
解析：“_”代表一个“任意字符”。所以总长度为 3，且开头两个字符为 DB。
考点：单表查询
41
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p044 =====


天行电力培训
15.C
解析：“_”代表“任意一个字符”，排除D选项。判断为空的语法是 IS NULL，不能用等号，因此选项A、B错误。
考点：单表查询
16.D
解析：目标是查看CITY，排除选项C。D中的DISTINCT，能去掉重复的城市，比A、B的效果好。因此最合适的是选项D。
考点：单表查询
17.B
解析：分组检索需要使用GROUP BY，GROUP BY后面只能用HAVING进行条件筛选。
考点：单表查询
18.B
解析：分组检索需要使用GROUP BY，GROUP BY后面只能用HAVING进行条件筛选。
考点：单表查询
19.B
解析：根据等值连接的相关规则，将R.A=S.A做连接操作。
考点：连接查询
20.A
解析：学生信息涉及S表，成绩涉及SC表。
考点：连接查询
21.C
解析：左外连接将保留左边有数值的元组，不关心右边是不是空值。
考点：连接查询
22.D
解析：嵌套查询上层的查询块称为外层查询或父查询，下层查询块称为内层查询或子查询。
考点：嵌套查询
23.B
解析：选项A缺少SSEX；选项C缺少IN谓词；选项D的LIKE是模糊查询。只有选项B正确。
考点：嵌套查询
24.C
解析：UPDATE用于更新表中的数据。
考点：数据更新（增加、删除、修改）
25.C
解析：选项A语法错误。数值型不应该带单引号，选项B错误。选项D中的110超出了（0,100），选项D错误。
考点：数据更新（增加、删除、修改）
26.A
解析：关键字DELETE用于数据删除。
考点：数据更新（增加、删除、修改）
27.D
解析：UPDATE <表名> SET <列名>=<表达式>[, <列名>=<表达式>]... [WHERE <条件>]
考点：数据更新（增加、删除、修改）
28.A
解析：ALTER是修改数据库，所以C错误。DELETE用来操作数据，而不是数据库，所以B错误。D表示
42
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p045 =====


天行电力培训

删除基本表。只有选项 A 正确。
考点：数据更新（增加、删除、修改）
29.A
解析：如果要求对视图更新时必须满足子查询中的条件表达式，应当在 CREATE VIEW 语句中使用短语
“WITH CHECK OPTION”。该短语表示对视 图进行 UPDATE、INSERT 和 DELETE 操作时，要保证更新、
插入或删除的行满足视图定义中的谓词条件（即子查询中的条件表达式），若不满足条件，则拒绝执行该
操作。
考点：视图（定义、查询、更新）
30.C
解析：视图机制可以让用户只看到特定的行和列。
考点：视图（定义、查询、更新）
二、多选题
1.BD
解析：选项 B 是数据库恢复中的“回滚”；GRANT 是数据控制语句，用于授权。
考点：SQL 基本概念、特点
2.AD
解析：可以没有索引，也可以不设置键。
考点：数据定义（模式、基本表、索引、数据字典）
3.ABCD
解析：查询数据可以来自一个或多个基本表和视图。
考点：单表查询
4.BD
解析：AND 是逻辑运算，IN 是集合运算。
考点：单表查询
5.ACD
解析：默认为 ASC，ASC 表示升序，所以选项 A、C 不正确；DICTINCT 用来去重，而不是排序，所以选
项 D 错误。
考点：单表查询
6.BC
解析：'_A%'表示第二个字符为 A 的所有字符串。
考点：单表查询
7.ABD
解析：LEFT JOIN 表示左外连接，将保留左边 STUDENT 有数值的元组，不关心右边 ENROLLMENT 是不
是空值。
考点：连接查询
8.BCD
解析：参加集合操作的各查询结果的列数必须相同，对应项的数据类型也必须相同。
考点：集合查询
9.CD
解析：A 选项没有限定范围；B 选项中的 OR 不能满足 55 分至 60 之间这个条件。
考点：数据更新（增加、删除、修改）
10.CD
解析：视图只是逻辑结构，没有存储，选项 A 错误；视图与并发无关，选项 B 错误。
考点：视图（定义、查询、更新）

43

CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 8-3-数据库计算机类讲义-p046 =====


三、判断题（A.正确 B.错误）
1.B
解析：SQL既是独立的语言，又是嵌入式语言。
考点：数据定义（模式、基本表、索引、数据字典）
2.B
解析：数据定义功能包括定义表、定义视图和定义索引。增、删、改、查属于数据操纵功能。
考点：数据定义（模式、基本表、索引、数据字典）
3.B
解析：索引虽然能够加速数据库查询，但需要占用一定的存储空间，当基本表更新时，索引要进行相应的维护，这些都会增加数据库的负担，因此要根据实际应用的需要有选择地创建索引。
考点：数据定义（模式、基本表、索引、数据字典）
4.A
解析：SELECT结果中的元组个数最多是原表元组的个数。
考点：单表查询
5.A
解析：一个表可以与其自己进行连接。因为所有属性名都是同名属性，必须使用“别名”。
考点：连接查询
6.B
解析：相关子查询：子查询的查询条件依赖于父查询。
考点：嵌套查询
7.B
解析：选择、投影、连接不是集合操作。
考点：集合查询
8.B
解析：两表间的关系删除，不影响两个表自身。
考点：数据更新（增加、删除、修改）
9.A
解析：必须先进行有效性检查，避免产生错误操作。
考点：视图（定义、查询、更新）
10.A
解析：视图是虚表，主要起到提高查找效率和提高安全性的作用。
考点：视图（定义、查询、更新）

44
CS 扫描全能王
3亿人都在用的扫描App
