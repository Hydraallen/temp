# OCR 中间产物 — 数据结构 · 第 二 章 线性表
> 来源：7-3-数据结构计算机类讲义.pdf  整章物理页(18, 34)

# ========== 整章完整转写（含知识点 + 专项练习/答案） ==========


===== 物理页 7-3-数据结构计算机类讲义-p018 =====


天行电力培训

第二章 线性表
第二章 知识点

【知识点2-1】线性表的定义
在一个线性表中数据元素的类型是相同的，或者说线性表是由同一类型的数据元素构成的线性结构。
线性表定义如下：
线性表是具有相同数据类型的n（n≥0）个数据元素的有限序列，通常记为：
（a₁, a₂, … a_{i-1}, a_i, a_{i+1}, …a_n），其中，n为表长，n=0时称为空表。
表中相邻元素之间存在着次序关系。将 a_{i-1} 称为 a_i 的直接前驱，a_{i+1} 称为 a_i 的直接后继。就是说：
对于a_i，当 i=2,…,n 时，有且仅有一个直接前驱 a_{i-1}；当i=1,2,…,n-1 时，有且仅有一个直接后继
a_{i+1}；而 a₁ 是表中第一个元素，它没有前驱，a_n 是最后一个元素，它没有后继。

【知识点2-2】线性表的顺序存储
线性表的顺序存储是指在内存中用地址连续的一块存储空间顺序存放线性表的各元素，用这种存储形
式存储的线性表称为顺序表。因为内存中的地址空间是线性的，因此，用物理上的相邻实现数据元素之间
的逻辑相邻关系是既简单又自然的。如图 2-1 所示。

0  1  ...  i-1  i  n-1  ...  MAXSIZE-1
data  a₁  a₂  ...  a_{i-1}  a_i  a_{i+1}  ...  a_n  ...  □
↑last
图2-1 线性表的顺序存储

设a_i的存储地址为Loc(a_i)，每个数据元素占d个存储单元，则第i个数据元素的地址为：
Loc(a_i)=Loc(a_1) + (i-1)*d  1≤i≤n

这就是说只要知道顺序表首地址和每个数据元素所占地址单元的个数就可求出第i个数据元素的地址
来，即顺序表具有按数据元素的序号随机存取的特点。

【知识点2-3】顺序表上基本运算的实现
1、初始化运算
顺序表的初始化即构造一个空表，这是一个加工型的运算，因此，将 L 设为指针参数，首先动态分配
存储空间，然后，将表中 last 指针置为-1，表示表中没有数据元素。
2、插入运算
线性表的插入是指在表的第 i 个位置上插入一个值为 x 的新元素，插入后使原表长为 n 的表成为表
长为 n+1 表。插入过程如图 2-2 所示。

16

CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 7-3-数据结构计算机类讲义-p019 =====


天行电力培训

0  1      i-2  i-1  i      n-1      MAXSIZE-1
a₁  a₂  ...  aᵢ₋₁  aᵢ  aᵢ₊₁  ...  aₙ  ...
a) 插入前

0  1      i-2  i-1  i  j+1      n      MAXSIZE-1
a₁  a₂  ...  aᵢ₋₁  x  aᵢ  aᵢ₊₁  ...  aₙ  ...
b) 插入后
图2-2 顺序表中的插入

插入前：(a₁, a₂, … , aᵢ₋₁, aᵢ, aᵢ₊₁, … , aₙ)
插入后：(a₁, a₂, … , aᵢ₋₁, x, aᵢ, aᵢ₊₁, … , aₙ)  其中：1≤i≤n+1
顺序表上完成这一运算的步骤如下：
1) 将 aᵢ～aₙ 顺序向下移动，为新元素让出位置；
2) 将 x 置入空出的第 i 个位置；
3) 修改 last 指针(相当于修改表长)，使之仍指向最后一个元素。
3、删除运算
线性表的删除运算是指将表中第 i 个元素从线性表中去掉，删除后使原表长为 n 的线性表成为表长为 n-1 的线性表：
删除前：(a1, a2, … , ai-1, ai, ai+1, … , an)
删除后：(a1, a2, … , ai-1, ai+1, … , an )  其中：1≤i≤n
删除过程如图2-3所示。

0  1      i-2  i-1  i      n-1      MAXSIZE-1
a₁  a₂  ...  aᵢ₋₁  aᵢ  aᵢ₊₁  ...  aₙ  ...
a) 删除前

0  1      i-2  i-1      n-2      MAXSIZE-1
a₁  a₂  ...  aᵢ₋₁  aᵢ₊₁  ...  aₙ  ...
b) 删除后
图2-3 顺序表中的删除

顺序表上完成这一运算的步骤如下：
1) 将 aᵢ₊₁～aₙ 顺序向前移动。
2) 修改 last 指针(相当于修改表长)使之仍指向最后一个元素。
4、按值查找
线性表中的按值查找是指在线性表中查找与给定值 x 相等的数据元素。在顺序表中完成该运算最简单的方法是：从第一个元素 a1 起依次和 x 比较，直到找到一个与 x 相等的数据元素，则返回它在顺序表中的存储下标或序号（二者差一，即序号=下标+1）；或者查遍整个表都没有找到与 x 相等的元素，返回-1。

17

CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 7-3-数据结构计算机类讲义-p020 =====


天行电力培训

【知识点2-4】线性表的链式存储  
由于顺序表的存储特点是用物理上的相邻实现了逻辑上的相邻，它要求用连续的存储单元顺序存储线性表中的各元素，因此，对顺序表插入、删除时需要通过移动数据元素来实现，影响了运行效率。线性表的链式存储结构不需要用地址连续的存储单元来实现，因为它不要求逻辑上相邻的两个数据元素物理上也相邻，它是通过“链”来表示数据元素之间的逻辑关系，因此对线性表的插入、删除不需要移动数据元素。  
1、单链表  
链表是通过一组任意的存储单元来存储线性表中的数据元素的，那么怎样表示出数据元素之间的线性关系呢？为建立起数据元素之间的线性关系，对每个数据元素a，除了存放数据元素的自身的信息a，之外，还需要和a，一起存放其后继元素a，所在的存储单元的地址，这两部分信息组成一个“结点”，结点的结构如图2-6所示，每个元素都如此。data域称为数据域，用来存放数据元素的信息；next域称为指针域，用来存放其后继元素的地址信息。  

图2-4 单链表结点结构  

因此n个元素的线性表可以通过每个结点的指针域拉成了一个“链”，称之为链表。因为每个结点中只有一个指向后继的指针，所以又称其为单链表。  
链表是由一个个结点构成的，结点类型如下：  
typedef struct lnode  
{ datatype data;  
  struct lnode *next;  
} LNode, *LinkList;  
定义头指针变量：  
LinkList H;  
2、循环链表  
对于单链表而言，最后一个结点的指针域是空指针，如果将该链表头指针置入该指针域，则使得链表头尾结点相连，就构成了单循环链表。如图2-5所示。在单循环链表上的操作基本上与非循环链表相同，只是将原来判断指针是否为NULL变为是否是头指针而已，没有其它较大的变化。  

图2-5 带头结点的单循环链表  
a)非空表 b)空表  

3、双向链表  
以上讨论的单链表的结点中只有一个指向其后继结点的指针域next，因此若已知某结点的指针为p，  

18  

CS 扫描全能王  
3亿人都在用的扫描App

===== 物理页 7-3-数据结构计算机类讲义-p021 =====


天行电力培训

其后继结点的指针则为p->next ，而找其前驱则只能从该链表的头指针开始，顺着各结点的next域进行，也就是说找后继的时间性能是O(1)，找前驱的时间性能是O(n)，如果也希望找前驱的时间性能达到O(1)，则只能付出空间的代价：每个结点再加一个指向前驱的指针域，结点的结构为如图2-6所示，用这种结点组成的链表称为双向链表。

图2-6 双向链表结点结构
┌───────┬───────┬───────┐
│ prior │  data │  next │
└───────┴───────┴───────┘

【知识点2-5】链表上基本运算的实现
1、建立单链表
（1）在链表的头部插入结点建立单链表
链表与顺序表不同，它是一种动态的存储结构，链表中每个结点占用的存储空间不是预先分配的，而是运行时系统根据需求生成的，因此建立单链表从空表开始，每读入一个数据元素则申请一个结点，然后插在链表的头部。
2）在单链表的尾部插入结点建立单链表
在头部插入建立单链表简单，但读入的数据元素的顺序与生成的链表中元素的顺序是相反的，若希望次序一致，则用尾插入的方法。因为每次是将新结点插入到链表的尾部。
2、求表长
设一个移动指针p和计数器j，初始化后，p所指结点后面若还有结点，p向后移动，计数器加1。
3、查找操作
（1）按序号查找 Get_Linklist(L,i)
算法思路：从链表的第一个元素结点起，判断当前结点是否是第i个，若是，则返回该结点的指针，否则继续后一个，表结束为止。没有第i个结点时返回空。
（2）按值查找即定位 Locate_LinkList(L,x)
算法思路：从链表的第一个元素结点起，判断当前结点值是否等于x，若是，返回该结点的指针，否则继续后一个，表结束为止。找不到时返回空。
4、插入操作
（1）后插结点
设p指向单链表中某结点，s指向待插入的值为x的新结点，将*s插入到*p的后面。
（2）前插结点
设p指向链表中某结点，s指向待插入的值为x的新结点，将*s插入到*p的前面。
5、删除操作
设p指向单链表中某结点，删除*p。
【知识点2-6】头结点及其作用
在链表中，附加在第一个元素结点之前的一个结点。作用是：使对空表和非空表的处理得到统一；在链表的第一个位置上的操作和在其他位置上的操作一致，无需特殊处理。

19

CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 7-3-数据结构计算机类讲义-p022 =====


【知识点2-7】顺序存储和链式存储的比较
顺序存储的优点是：
1) 方法简单，各种高级语言中都有数组类型，容易实现。
2) 不用为表示结点间的逻辑关系而增加额外的存储开销。逻辑相邻与物理相邻一致。
3) 顺序表具有按元素序号随机访问的特点。
顺序存储的缺点是：
1) 顺序表中做插入删除操作时，平均移动大约表中一半的元素，因此对n较大的顺序表插入、删除的效率低。
2) 需要预先分配足够大的存储空间，估计过大，可能会导致顺序表后部大量闲置；预先分配过小，又会造成溢出。插入和删除运算平均需移动大约表中一半的元素；存储空间是静态分配的，在程序执行之前必须明确规定存储规模，因此分配不足则会造成溢出，分配过大，又可能造成存储空间的浪费。
链式存储的优点是：
1) 在顺序表中做插入删除操作时，不需要多次移动的元素，因此对n较大的顺序表插入和删除的效率高。
2) 不需要预先分配足够大的存储空间。
链式存储的缺点是：
1)需要为存储结点间的逻辑关系而增加额外的存储开销。
2)链表不具有按元素序号随机访问的特点。
3)存储密度降低。
20
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 7-3-数据结构计算机类讲义-p023 =====


天行电力培训

第二章 专项练习

一、单选题
1、线性表是（ ）。
A.一个有限序列，可以为空
B.一个有限序列，不能为空
C.一个无限序列，可以为空
D.一个无序序列，不能为空
2、对顺序存储的线性表，设其长度为n，在任何位置上插入或删除操作都是等概率的，插入一个元素时平均要移动表中的（ ）个元素。
A. n/2
B. n+1/2
C. n-1/2
D. n
3、线性表采用链式存储时，其地址（ ）。
A.必须是连续的
B.部分地址必须是连续的
C.一定是不连续的
D.连续与否均可以
4、用链表表示线性表的优点是（ ）。
A.便于随机存取
B.花费的存储空间较顺序存储少
C.便于插入和删除
D.数据元素的物理顺序与逻辑顺序相同
5、链表中最常用的操作是在最后一个元素之后插入一个元素和删除最后一个元素，则采用（ ）存储方式最节省运算时间。
A.单链表
B.双链表
C.单循环链表
D.带头结点的双循环链表
6、下面关于线性表的叙述错误的是（ ）。
A.线性表采用顺序存储，必须占用一片地址连续的单元
B.线性表采用顺序存储，便于进行插入和删除操作
C.线性表采用链式存储，不必占用一片地址连续的单元
D.线性表采用链式存储，便于进行插入和删除操作

21

CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 7-3-数据结构计算机类讲义-p024 =====


天行电力培训
7、单链表中，增加一个头结点的目的是为了（ ）。
A. 使单链表至少有一个结点
B. 标识表结点中首结点的位置
C. 方便运算的实现
D. 说明单链表是线性表的链式存储
8、在单链表指针为p的结点之后插入指针为s的结点，正确的操作是（ ）。
A. p->next=s; s->next=p->next;
B. s->next=p->next; p->next=s;
C. p->next=s; p->next=s->next;
D. p->next=s->next; p->next=s;
9、在双向链表存储结构中，删除p所指的结点时须修改指针（ ）。
A. (p->prior)->next=p->next; (p->next)->prior=p->prior;
B. p->prior=(p->prior)->prior; (p->prior)->next=p;
C. (p->next)->prior=p; p->rlink=(p->next)->next;
D. p->next=(p->prior)->prior; p->prior=(p->next)->next;
10、完成在双向循环链表结点p之后插入s的操作是（ ）。
A. p->next=s; s->prior=p; p->next->prior=s; s->next=p->next;
B. p->next->prior=s; p->next=s; s->prior=p; s->next=p->next;
C. s->prior=p; s->next=p->next; p->next=s; p->next->prior=s;
D. s->prior=p; s->next=p->next; p->next->prior=s; p->next=s;
11、若某线性表中最常用的操作是取第i个元素和找第i个元素的前驱元素，则采用（ ）存储方式最节省运算时间。
A. 单链表
B. 顺序表
C. 双链表
D. 单循环链表
12、单链表的存储密度（ ）。
A. 大于1
B. 等于1
C. 小于1
D. 不能确定
13、设单循环链表中结点的结构为(data, link)，且first为指向链表表头的指针，current为链表当前指针，在循环链表中检测current是否达到链表表尾的语句是( )。
A. current->link=null
22
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 7-3-数据结构计算机类讲义-p025 =====


20、下列对顺序存储的有序表（长度为 n）实现给定操作的算法中，平均时间复杂度为 O(1)的是：

===== 物理页 7-3-数据结构计算机类讲义-p026 =====


天行电力培训

（ ）。

A. 查找包含指定值元素的算法
B. 插入包含指定值元素的算法
C. 删除第 i (1≤i≤n) 个元素的算法
D. 获取第 i (1≤i≤n) 个元素的算法
二、多选题
1、从表中任一结点出发都能扫描整个表的是（ ）。

A. 静态链表
B. 单链表
C. 循环链表
D. 双链表
2、设一条单链表的头指针变量为 head 且该链表没有头结点，则不能作为其判空条件是（ ）。

A. head==NULL
B. head->next=NULL
C. head->next==head
D. head!=NULL
3、下列关于链式存储结构，哪些是正确的（ ）。

A. 结点除自身信息外还包括指针域，因此存储密度小于顺序存储结构
B. 逻辑上相邻的结点物理上不必邻接
C. 可以通过计算直接确定第 i 个结点的存储地址
D. 插入、删除操作方便，不必移动结点
4、线性表的两种存储结构叙述正确的是（ ）。

A. 线性表顺序存储结构可以随机存取表中任一元素
B. 线性表链式存储结构只能顺序存取表中任一元素
C. 线性表顺序存储结构在插入或删除某一元素时，需要移动大量元素
D. 线性表链式存储结构在插入或删除某一元素时，需要移动大量元素
5、下列叙述哪些是正确的（ ）。

A. 线性表的逻辑顺序与物理顺序总是一致的
B. 线性表的顺序存储表示优于链式存储表示
C. 线性表若采用链式存储表示时所有结点之间的存储单元地址可连续可不连续
D. 二维数组是每个元素都为顺序表的顺序表
6、下列关于线性表中的链表描述正确的是（ ）。

A. 适用于数据项数量不能预知的情况
B. 逻辑相邻的两元素的存储空间可以是不连续的

24

===== 物理页 7-3-数据结构计算机类讲义-p027 =====


天行电力培训
C. 链表节点一般有数据元素和指针域两部分组成
D. 存储空间需要动态分配
7、便于插入和删除操作的是（ ）。
A. 静态链表
B. 单链表
C. 双链表
D. 循环链表
8、下列哪些是线性表的基本操作（ ）。
A. 构造线性表
B. 销毁线性表
C. 将元素插入线性表
D. 初始化线性表
9、线性结构的特点是（ ）。
A. 集合中必存在唯一的一个“第一元素”
B. 集合中必存在唯一的一个“最后元素”
C. 除最后元素外，均有唯一的后继
D. 除第一元素之外，均有唯一的前驱。
10、下面关于线性表的叙述正确的是（ ）。
A. 线性表采用顺序存储必须占用一片连续的存储空间
B. 线性表采用链式存储不必占用一片连续的存储空间
C. 线性表采用链式存储便于插入和删除操作的实现
D. 线性表采用顺序存储便于插入和删除操作的实现
11、线性表的两种存储结构叙述正确的是（ ）。
A. 线性表顺序存储结构可以随机存取表中任一元素
B. 线性表链式存储结构只能顺序存取表中任一元素
C. 线性表顺序存储结构在插入或删除某一元素时，需要移动大量元素
D. 线性表链式存储结构在插入或删除某一元素时，不需要移动大量元素
12、下面的叙述不正确的是（ ）。
A. 线性表在链式存储时，查找第 i 个元素的时间同 i 值无关
B. 线性表在链式存储时，查找第 i 个元素的时间同 i 值成正比
C. 线性表在顺序存储时，查找第 i 个元素的时间同 i 值无关
D. 线性表在顺序存储时，查找第 i 个元素的时间同 i 值成正比
三、判断题（A. 正确  B. 错误）
1、线性表的逻辑顺序与存储顺序总是一致的。（ ）
25

===== 物理页 7-3-数据结构计算机类讲义-p028 =====


天行电力培训
2、顺序存储的线性表可以按序号随机存取。（ ）
3、顺序表的插入和删除操作不需要付出很大的时间代价，因为每次操作平均只有近一半的元素需要移动。（ ）
4、线性表中的元素可以是各种各样的，但同一线性表中的数据元素具有相同的特性，因此属于同一数据对象。（ ）
5、在线性表的顺序存储结构中，逻辑上相邻的两个元素在物理位置上并不一定相邻。（ ）
6、在线性表的链式存储结构中，逻辑上相邻的元素在物理位置上不一定相邻。（ ）
7、线性表的链式存储结构优于顺序存储结构。（ ）
8、在线性表的顺序存储结构中，插入和删除时移动元素的个数与该元素的位置有关。（ ）
9、线性表的链式存储结构是用一组任意的存储单元来存储线性表中数据元素的。（ ）
10、在单链表中要取得某个元素只要知道该元素的指针即可，所以单链表是随机存取的存储结构。（ ）
11、线性表的特点是每个元素都有一个前驱和一个后继。（ ）
12、顺序存储方式只能用于存储线性结构。（ ）

26
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 7-3-数据结构计算机类讲义-p029 =====


天行电力培训
第二章 参考答案及解析
一、单选题
1、A
解析：线性表是具有相同数据类型的n (n≥0)个数据元素的有限序列，通常记为：
(al, a2, … ai-1, ai, ai+1, …an)，其中，n为表长，n=0时称为空表。
考点：2-1线性表的定义
2、A
解析：根据顺序表插入运算的性能分析可得，等概率情况下，插入一个时平均要移动表中的n/2个元素。
考点：2-3顺序表上基本运算的实现
3、D
解析：链式存储时，分配的结点地址任意，连续与否均可。
考点：2-4线性表的链式存储
4、C
解析：在顺序表中做插入删除操作时，不需要多次移动的元素，因此对n较大的顺序表插入和删除的效率高。
考点：2-7顺序存储和链式存储的比较
5、D
解析：采用单循环链表，寻找最后一个元素和最后一个元素的后面一个元素（即链表的第一个元素）都可以使用尾指针直接得到，算法时间复杂度是O(1)，是最节省运算时间的。
考点：2-4线性表的链式存储、2-5链表上基本运算的实现
6、B
解析：线性表采用顺序存储时，插入和删除操作都要移动大量元素，不如链式存储方便，故错误。
考点：2-3顺序表上基本运算的实现、2-5链表上基本运算的实现
7、C
解析：单链表的有些操作中，第一个结点的处理和其它结点是不同的，原因是第一个结点加入时链表为空，它没有直接前驱结点，它的地址就是整个链表的指针，需要放在链表的头指针变量中；而其它结点有直接前驱结点，其地址放入直接前驱结点的指针域。“第一个结点”的问题在很多操作中都会遇到，如在链表中插入结点时，将结点插在第一个位置和其它位置是不同的，在链表中删除结点时，删除第一个结点和其它结点的处理也是不同的，等等，为了方便操作，有时在链表的头加入一个“头结点”，头结点的类型与数据结点一致，标识链表的头指针变量L中存放该结点的地址，这样即使是空表，头指针变量L也不为空了。头结点的加入使得“第一个结点”的问题不再存在，也使得“空表”和“非空表”的处理成为一致。头结点的加入完全是为了运算的方便，它的数据域无定义，指针域中存放的是第一个数据结点的地址，空表时为空。
考点：2-6头结点及其作用
27
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 7-3-数据结构计算机类讲义-p030 =====


天行电力培训
8、B
解析：注意两个指针的操作顺序不能颠倒。
考点：2-5 链表上基本运算的实现
9、A
解析：双向链表中，删除p所指的结点时的过程如图所示
考点：2-5 链表上基本运算的实现
10、D
解析：双向链表中在结点p之后插入s的过程如图所示
考点：2-5 链表上基本运算的实现
11、B
解析：顺序表可以采用下标的方式直接存取第i个元素和第i元素的前驱元素，时间复杂度为O(1)，比链式存储要节省时间。
考点：2-7 顺序存储和链式存储的比较
12、C
解析：为链表结点所分配的空间中，不仅要存放数据，还要为保存关系开销部分空间，所以导致存储密度小于1。
考点：2-4 线性表的链式存储
13、D
解析：单循环链表中最后一个数据结点的指针域指向表头，D选项的描述符合要求。
考点：2-4 线性表的链式存储
14、B
解析：引入头结点的目的就是为了统一运算，对空间的节省和运算速度的提升并没有作用。
考点：2-4 线性表的链式存储
15、C
28
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 7-3-数据结构计算机类讲义-p031 =====


天行电力培训

解析：此操作为将后表的第一个数据结点链接在前表的最后一个数据结点的后边即可。长度为m的单链表在前，长度为n的单链表在后，寻找前表的最后一个结点的时间复杂度为m，后表的第一个数据结点可以直接通过头指针得到，所以总体时间复杂度与后表的长度无关。
考点：2-5 链表上基本运算的实现
16、C
解析：根据线性表的定义可知。
考点：2-1 线性表的定义
17、C
解析：随机查找元素的时间复杂度不论是顺序表还是链表都是O(n)。
考点：2-3 顺序表上基本运算的实现、2-5 链表上基本运算的实现
18、B
解析：链式结构在实现删除插入操作的效率较高，当插入删除操作比较频繁的时候，选用链式结构比较合适。
考点：2-7 顺序存储和链式存储的比较
19、A
解析：顺序表使用数组进行存储，可以使用数组下标的方式对数据进行随机存取。
考点：2-2 线性表的顺序存储
20、D
解析：在顺序表中，A、B、C对应的算法的平均时间复杂度均为O(n)，D对应的法为O(1)，故选D。
考点：2-3 顺序表上基本运算的实现
二、多选题
1、CD
解析：静态链表和单链表都是单向的，如果不从头指针出发的话是不能扫描到整个表的。
考点：2-4 线性表的链式存储
2、BCD
解析：没有头结点的单链表的判空条件是head == NULL，其他三个选项都不是，故选BCD。
考点：2-5 链表上基本运算的实现
3、ABD
解析：链式存储结构里要确定第i个结点的存储地址只能从头指针开始“顺藤摸瓜”，不能像顺序存储结构那样直接计算得出。
考点：2-4 线性表的链式存储
4、ABC
解析：链式存储结构中做插入和删除操作时是不需要元素的，D的说法错误，故选ABC。
考点：2-2 线性表的顺序存储、2-4 线性表的链式存储

29

CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 7-3-数据结构计算机类讲义-p032 =====


天行电力培训
5、CD
解析：链表的逻辑顺序与物理顺序可能是不一致的，不同数据结构的优劣要基于不同的条件和场景才能够比较，没有绝对的优劣。
考点：2-1 线性表的定义、2-2 线性表的顺序存储、2-4 线性表的链式存储、2-7 顺序存储和链式存储的比较
6、ABCD
解析：链表的存储空间是动态分配的，而顺序表的存储空间是静态分配的。
考点：2-4 线性表的链式存储、2-5 链表上基本运算的实现
7、ABCD
解析：静态链表采用链式的管理思想，插入删除操作也不需要移动元素，BCD 里的结构均为链表，便于插入和删除。
考点：2-4 线性表的链式存储、2-5 链表上基本运算的实现
8、ABCD
解析：ABCD 选项中描述的都是关于线性表的基本操作。
考点：2-1 线性表的定义、2-3 顺序表上基本运算的实现、2-5 链表上基本运算的实现
9、ABCD
解析：ABCD 的描述均为线性结构的特点。
考点：2-1 线性表的定义
10、ABC
解析：顺序表做插入和删除操作时需移动大量的元素，并不便于操作。
考点：2-1 线性表的定义、2-2 线性表的顺序存储、2-4 线性表的链式存储
11、ABCD
解析：顺序表可以随机存取，也可以顺序存取，链表只能顺序存取；顺序表做插入和删除操作时需移动大量的元素，而链表不需要。
考点：2-2 线性表的顺序存储、2-4 线性表的链式存储
12、AD
解析：线性表在链式存储时，查找元素需要从第一个结点开始顺次往下找，i 值越大，指针移动的次数越多，所以消耗时间同 i 值成正比，线性表在顺序存储时，查找元素可以根据 i 的下标值直接存取，与 i 值无关，所以 BC 正确，AD 不正确，故选 AD。
考点：2-2 线性表的顺序存储、2-4 线性表的链式存储
三、判断题（A. 正确  B. 错误）
1、B
解析：链表的逻辑顺序与存储顺序可能是不一致的。
考点：2-1 线性表的定义
30
CS 扫描全能王
3 亿人都在用的扫描 App

===== 物理页 7-3-数据结构计算机类讲义-p033 =====


天行电力培训
2、A
解析：顺序表采用数组作为数据存储空间，可以借助数组下标进行随机存取。
考点：2-2线性表的顺序存储
3、B
解析：移动约一半的元素跟链表比起来已经是很大的时间代价了。
考点：2-3顺序表上基本运算的实现
4、A
解析：线性表中的数据元素可以是基础类型，也可以是组合类型，但是要具有相同的特性，属于同一数据对象。
考点：2-1线性表的定义
5、B
解析：顺序存储结构中逻辑相邻的两个元素在物理位置上一定相邻。
考点：2-2线性表的顺序存储
6、A
解析：链式存储结构中，结点的存储空间会由系统随机分配，所以物理位置上不一定相邻。
考点：2-4线性表的链式存储
7、B
解析：每个结构没有绝对的优劣，要结合具体环境去进行比较。
考点：2-7顺序存储和链式存储的比较
8、A
解析：顺序表插入删除操作需要移动元素，元素位置不同移动元素的次数也不同，所以相关。
考点：2-3顺序表上基本运算的实现
9、A
解析：此处的任意指的是地址上的任意，不必连续。
考点：2-4线性表的链式存储
10、B
解析：如何知道该元素的指针呢？得从头指针开始顺次往下找，所以不能算是随机存取。
考点：2-4线性表的链式存储
11、B
解析：第一个元素没有前驱，最后一个元素没有后继。
考点：2-1线性表的定义
12、B
解析：顺序存储不仅可以存储线性表、栈、队列这样的线性结构，也可以存储诸如树和图这样的非线性结构。
31
CS 扫描全能王
3亿人都在用的扫描App

===== 物理页 7-3-数据结构计算机类讲义-p034 =====


天行电力培训
考点：2-2线性表的顺序存储、5-6二叉树的存储、6-6、6-7、6-8、6-9图的存储
天行电力培训
32
CS 扫描全能王
3亿人都在用的扫描App
